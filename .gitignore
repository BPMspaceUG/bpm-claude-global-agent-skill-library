# System and editor files
.DS_Store
Thumbs.db
.idea/
.vscode/

# Secrets and environment
.env
.env.*
*.key
*.pem
*.p12

# Claude local state (do not commit)
.claude.json
claude.json
settings.json

# Bootstrap (script is gitignored; .env contains secrets)
bpm-bootstrap.sh
bpm-bootstrap.env

# Build and temp directories
tmp/
dist/